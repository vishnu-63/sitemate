export declare class RMQConfig {
    host: string;
    port: number;
    constructor(values?: {
        host: string;
        port: number;
    } | undefined);
}
export declare const rmqConfig: RMQConfig;
